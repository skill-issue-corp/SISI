name: Hunt for Heisentests

on:
  workflow_dispatch:
  # every 60 minutes
  schedule:
    - cron: "*/60 * * * *"

jobs:
  hunt:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.get_fails.outputs.matrix }}
      count: ${{ steps.get_fails.outputs.count }}

    steps:
    - name: Checkout Master
      uses: actions/checkout@v7
      with:
          submodules: recursive

    - name: Install dependencies
      run: dotnet restore

    - name: Build Project
      run: dotnet build --configuration DebugOpt --no-restore /m

    # never seen map heisentest and gamerule tests are way too flaky right now to even care
    - name: Run Content.IntegrationTests
      id: integration_tests
      run: |
        DOTNET_gcServer=1 dotnet test --no-build --configuration DebugOpt \
        Content.IntegrationTests/Content.IntegrationTests.csproj -- \
        NUnit.Where="cat != MapTests && cat != GameRuleTests" \
        NUnit.ConsoleOut=0 \
        NUnit.MapWarningTo=Failed \
        NUnit.TestOutputXml="logs" \
        NUnit.WorkDirectory="$(pwd)/test_results"

    - name: Archive NUnit3 test results.
      if: failure()
      uses: actions/upload-artifact@v7
      with:
        name: nunit3-results
        path: test_results/*
        retention-days: 7
        compression-level: 9

    - name: Extract Failures
      if: failure()
      id: get_fails
      run: |
        python -m pip install --upgrade pip
        pip install xmltodict
        python Tools/actions_extract_failures.py

  report:
    needs: [ hunt ]
    runs-on: ubuntu-latest
    permissions:
      issues: write
    strategy:
      matrix:
        value: ${{ fromJSON(needs.hunt.outputs.matrix) }}
    if: ${{ always() && failure() && (needs.hunt.outputs.count < 5) }}
    name: Error for ${{ matrix.value.name }}
    steps:
      - name: Checkout Master
        uses: actions/checkout@v7

      - name: Create Issue
        uses: dblock/create-a-github-issue@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NAME: ${{ matrix.value.name }}
          FULLNAME: ${{ matrix.value.fullname }}
          OUTPUT: ${{ matrix.value.output }}
          FAILURE: ${{ matrix.value.failure }}
        with:
          filename: .github/ISSUE_TEMPLATE/heisentest_template.md
          update_existing: true
          search_existing: all
